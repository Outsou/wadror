<h2>List of ratings</h2>

<p>
  Number of ratings: <%= @ratings.count %>
</p>

<p>
  <ul class="list-group">
    <% @ratings.each do |rating| %>
        <li class="list-group-item"> <%= rating %> <%= link_to rating.user.username, rating.user, class:"btn btn-primary" %> </li>
    <% end %>
  </ul>
</p>

<p>
  <%= link_to 'New rating', new_rating_path %>
</p>